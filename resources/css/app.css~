@tailwind base;
@tailwind components;
@tailwind utilities;

body{
    @apply min-h-screen;
    @apply bg-gray-100;
    @apply text-gray-800;
}

.container__25 {
    @apply flex flex-col items-center pt-6 sm:justify-center sm:pt-0 w-full overflow-hidden  px-6 py-4 shadow-md sm:max-w-lg sm:rounded-lg;
    @apply mx-auto mt-28; /* Alineación y margen superior */
    vertical-align: middle;
    @apply grid place-items-center;
    @apply bg-white;
    @apply w-full;

    /* Agregar tamaños responsivos */
    @apply md:max-w-xl; /* Para dispositivos medianos */
    @apply lg:w-full; /* Para dispositivos grandes */
    @apply xl:w-full; /* Para dispositivos extra grandes */
}


.container__75{
    @apply flex;
    @apply items-center;
    @apply w-screen;
    @apply flex-col;
    @apply place-items-center;
    @apply mt-6;
    @apply w-11/12;
    @apply overflow-hidden;
    @apply  bg-white;
    @apply px-6;
    @apply py-4;
    @apply shadow-md;
    @apply mx-auto;
    @apply rounded-lg;
    @apply bg-white;
}

.nav__bar{
    @apply navbar;
    @apply shadow-md;
    @apply bg-white;
}
.nav__bar__button{
    @apply btn;
    @apply btn-ghost;
}

.input__label {
    @apply font-semibold;
    @apply input input-bordered; /* Combinar clases de Tailwind */
    @apply flex items-center; /* Flexbox para alinear elementos */
    @apply bg-transparent; /* Fondo transparente */
    @apply w-full; /* Ancho completo */
    @apply mb-2; /* Margen inferior para separación de inputs */
}

.input__data__entry {
    @apply font-medium; /* Fuente media */
    @apply border-none; /* Sin borde */
    @apply bg-transparent; /* Fondo transparente */
    @apply w-full; /* Ancho completo */
    @apply p-2; /* Espaciado interno para los inputs */
    @apply placeholder:text-gray-400; /* Color del texto de los placeholders */
}

/* Estilos responsivos */
@media (min-width: 640px) { /* sm */
    .input__label {
        @apply mb-4; /* Margen inferior mayor en pantallas más grandes */
    }
    .input__data__entry {
        @apply text-lg; /* Aumentar el tamaño del texto en pantallas más grandes */
    }
}

@media (min-width: 768px) { /* md */
    .input__data__entry {
        @apply text-xl; /* Aumentar aún más el tamaño del texto */
    }
}


button{
    &:hover{
        color: white;
    }
}

.employee__table{
    @apply table;
    @apply table-zebra;
}

.logo{
    @apply text-5xl font-extrabold justify-end content-end;
    @apply ml-auto mr-4;

}
